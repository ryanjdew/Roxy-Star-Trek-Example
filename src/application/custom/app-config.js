//app-config.js - override /application/app-config.js configurations here

// example of using a different search options (/v1/confiq/query)
//$.extend( controller_config, {optionsNode: "newsearchoptions"} )

// example of defining a proxy with controller
//$.extend( controller_config, {proxy: "someproxy.com:8080"})

// example of changing default page length for results widget
//$.extend( controller_config, {pageLength: 4})

// example of changing titles for bar chart
//$.extend( bar_config_1, {
//     title: "Awards I Won",
//     dataLabel: "data label",
//     measureLabel:"measureLabel"
//     })

// example of changing title for pie chart
//$.extend( pie_config_2, {title: "People Title"})

// example of changing look and feel of heatmaps map widget config
//$.extend(map_config_1,
//{heatmap:{
//    radiusFactor:10,
//    opacity:30,
//    gradient:{ 0.45: "blue", 0.55: "green", 0.65: "yellow", 0.95: "orange", 1.0: "red" }, 
//    max:25
//    }
//});
